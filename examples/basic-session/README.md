# Basic session

```bash
SURFGATE_API_KEY='sg_test_...' node examples/basic-session/index.mjs
```

The example prints the provider-neutral session response, waits for Enter, and terminates the session. Live provider allocation requires the server's Cloudflare configuration.
